create database gdse69;

use gdse69;

create table customer(
    id varchar(5) primary key,
    name varchar(255),
    address varchar(255),
    contact varchar(255),
    nic varchar(255),
    email varchar(255),
    salary varchar(255),
    age varchar(255)
);